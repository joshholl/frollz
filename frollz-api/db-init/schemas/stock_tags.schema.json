{
  "rule": {
    "$schema": "http://json-schema.org/draft-07/schema",
    "type": "object",
    "properties": {
      "tagKey": {
        "type": "string",
        "minLength": 1,
        "description": "Reference to a tags document _key"
      },
      "stockKey": {
        "type": "string",
        "minLength": 1,
        "description": "Reference to a stocks document _key"
      },
      "createdAt": {
        "type": "string",
        "format": "date-time"
      }
    },
    "required": ["tagKey", "stockKey", "createdAt"],
    "additionalProperties": false
  },
  "level": "strict",
  "message": "Document does not conform to the stock_tags schema."
}
