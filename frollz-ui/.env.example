# API base URL. /api (relative) works for both the combined production container
# and the Vite dev server (which proxies /api to the API). Override only if
# you have a non-standard setup.
VITE_API_URL=/api